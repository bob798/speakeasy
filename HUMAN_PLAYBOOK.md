# Speakeasy Human 操作手册
> 这是你的操作指南。AI 负责所有书写和执行，你只需要在固定时机做决策和确认。
> 每个操作都标注了预计耗时。

---

## 一、你的核心职责

```
你负责：方向 · 决策 · 验收
AI负责：文档 · 代码 · 测试 · 状态同步
```

**你不需要做的事**（AI自动完成）：
- 写 SPEC 和 Instructions
- 更新 CLAUDE.md 的 Step 进度
- 维护 C4 架构图
- 写测试代码
- 更新 BUG_LOG 状态

---

## 二、固定操作流程

### 2.1 启动一个新版本（每个版本做一次，约 15 分钟）

**第一步：用自然语言描述想法**

不需要写正式文档，直接告诉 Claude 你想做什么。格式参考：

```
我要开始开发 Speakeasy V0.X。

我想做的事：
  [用1-3句话描述，说清楚用户能得到什么，不需要说怎么实现]
  例："我想让用户能看到自己练了多少天，有成就感"
  例："我想让 Alex 能记住用户喜欢聊什么话题"

请按 CLAUDE_CODE_DOC_STANDARD.md v2.1 流程进行。
```

**第二步：确认 User Stories（约 3 分钟）**

Claude 会把你的描述整理成标准 Story 格式，你只需要判断：
- 这个 Story 准确描述了我的想法吗？
- 有没有遗漏重要的用户目标？

回复"确认"或"第X条改成……"即可。

**第三步：确认 BDD Scenarios（约 5 分钟）**

Claude 会列出所有场景，你需要判断：
- 这些场景覆盖了所有用户使用路径吗？
- 有没有"用户会这么用但没有写出来"的情况？

⚠️ 这是最关键的确认环节。BUG-002 就是因为跳过这步导致的。

**第四步：把 Instructions 交给 Claude Code 执行**

把 Claude 生成的 `CLAUDE_CODE_VXX_INSTRUCTIONS.md` 直接交给 Claude Code，说：
```
请执行 CLAUDE_CODE_VXX_INSTRUCTIONS.md
```

---

### 2.2 版本开发中（每个 Step 完成后，约 1-2 分钟）

Claude Code 每完成一个 Step 会自动打印进度并更新 CLAUDE.md。

你只需要：
- 看到 `✅ Step N 完成` 后，确认没有异常
- 如果 Claude Code 停下来提问，给出决策即可

**不需要** 主动查看进度，Claude Code 会汇报。

---

### 2.3 版本验收（每个版本做一次，约 30 分钟）

Claude Code 完成所有 Step 后会输出汇报，你需要完成 6 项人工验收：

**自动化已覆盖的部分**（看报告即可）：
```
□ pytest 全部通过（看 Claude Code 汇报的测试结果）
□ Alex 行为测试 D1 全部通过（看 test_alex_behavior 报告）
```

**需要你手动验收的部分**（打开浏览器操作）：
```
□ 启动服务，走一遍主流程（对话 → 结束 → 看复盘）
□ 从历史记录进入旧对话，验证复盘按钮显示/隐藏逻辑
□ 点击消息中的词，验证追问 UI 弹出
□ Alex 的回复：有没有任何形式的显性纠错？（说出来、夸赞式引用都算）
```

**验收通过后**：
```
□ 更新 ROADMAP.md：把本版本从"进行中"移到"已发布"（1分钟）
□ 告诉 Claude："V0.X 验收通过，准备开始 V0.X+1"
```

---

### 2.4 确认 ADR（每次 Claude 提示时，约 30 秒/条）

当 Claude Code 做了重要技术决策，会生成 ADR 草稿并在汇报中标注"待确认"。

你只需要读一遍，确认：
- 背景描述准确吗？
- 决策是你认可的吗？

回复"确认"即可。不需要自己写 ADR。

---

### 2.5 发现 Bug（随时）

发现 Bug 时，告诉 Claude：
```
发现 Bug：[描述现象]
复现步骤：[1. 2. 3.]
```

Claude 会：
1. 在 BUG_LOG.md 中记录
2. 生成修复指令
3. 交给 Claude Code 执行

你不需要分析根因，只需要描述现象。

---

## 三、文档速查

| 文档 | 用途 | 你需要读的时机 |
|---|---|---|
| `CLAUDE.md` | AI 上岗手册，项目全貌 | 每次启动新对话时让 Claude 先读 |
| `ROADMAP.md` | 版本计划和废弃决策 | 版本验收后更新；有人问"为什么不做X"时查 |
| `docs/adr/` | 技术决策记录 | 有人质疑某个技术选择时查 |
| `BUG_LOG.md` | Bug 记录 | 发现 Bug 时查是否重复；版本验收时确认清零 |
| `SPEC_VXX.md` | 当前版本规格 | 对某个功能有疑问时查 |
| `ALEX_BEHAVIOR_TEST_STANDARD.md` | Alex 测试标准 | 觉得 Alex 行为有问题时查 |

---

## 四、启动新对话的标准方式

每次打开新的 Claude 对话，先发这条消息：

```
请阅读以下文件，然后等待我的指令：
- CLAUDE.md
- ROADMAP.md
- 最新的 SPEC_VXX.md

阅读完成后告诉我：当前版本是什么，进度到哪了。
```

这样 Claude 能在 30 秒内重建完整项目上下文，不需要你重复解释背景。

---

## 五、决策触发清单

遇到以下情况时，需要你做决策，AI 会停下来等你：

```
需求类
  · Claude 展示 BDD Scenarios，等你确认场景完整性
  · Claude 整理 User Stories，等你确认方向正确
  · 某个功能的实现有多种方案，影响产品体验

技术类（罕见，Claude 通常自行决策并记录 ADR）
  · 新引入外部依赖（新的 pip 包或第三方服务）
  · 数据表结构有破坏性变更

优先级类
  · 版本开发中途发现新需求，是否纳入当前版本
```

遇到 Claude Code 自行决策了不该它决策的事，直接说"这个需要问我"并告知正确答案。

---

## 六、版本节奏参考

```
启动版本   →  15分钟（描述需求 + 确认 Story + 确认 Scenarios）
开发执行   →  Claude Code 自动（你只需响应提问）
版本验收   →  30分钟（看报告 + 手动测试 + 更新 ROADMAP）

两次人工操作之间，项目完全由 AI 推进。
```